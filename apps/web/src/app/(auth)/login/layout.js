export const metadata = {
    title: "Sign In",
    description: "Sign in to your FlexFlow workspace and continue managing projects with your team.",
};

export default function LoginLayout({ children }) {
    return children;
}
