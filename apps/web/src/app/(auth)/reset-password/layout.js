export const metadata = {
    title: "Reset Password",
    description: "Create a new password for your FlexFlow account.",
    robots: { index: false },
};

export default function ResetPasswordLayout({ children }) {
    return children;
}
