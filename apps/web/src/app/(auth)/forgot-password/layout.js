export const metadata = {
    title: "Forgot Password",
    description: "Reset your FlexFlow password. Enter your email and we'll send you a secure link.",
    robots: { index: false },
};

export default function ForgotPasswordLayout({ children }) {
    return children;
}
