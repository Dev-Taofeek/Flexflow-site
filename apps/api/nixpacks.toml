[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["corepack enable pnpm", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm prisma generate"]

[start]
cmd = "pnpm prisma db push && node src/server.js"
