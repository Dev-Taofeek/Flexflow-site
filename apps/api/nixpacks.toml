[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm prisma generate"]

[start]
cmd = "pnpm prisma migrate deploy && node src/server.js"
