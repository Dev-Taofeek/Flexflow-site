{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "corepack enable pnpm && pnpm install --no-frozen-lockfile && pnpm prisma generate"
    },
    "deploy": {
        "startCommand": "pnpm prisma migrate deploy && node src/server.js",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 3,
        "healthcheckPath": "/"
    }
}
